autoclicker
Lightweight, configurable autoclicker written in Python.

A small tool to automate mouse clicks with configurable intervals, click type, number of clicks or duration, and optional hotkey control. Intended for productivity/testing tasks where repetitive clicking is needed. (Replace this short description with your project's exact purpose.)

Features
Start/stop with a hotkey (if implemented)
Configurable click interval (fixed or randomized)
Click type: left / right / double (if implemented)
Option to run for a set number of clicks or a duration
CLI and/or GUI mode (replace with actual modes implemented)
Cross-platform support: Windows / macOS / Linux (adjust per repo support)
Requirements
Python 3.8+ (replace with required version)
pip
Optional (add actual dependencies in requirements.txt):

pyautogui
keyboard
tkinter (for GUI)
pyinstaller (for building an executable)
Installation
Clone the repository: git clone https://github.com/veeti-21/autoclicker.git cd autoclicker

Create a virtual environment and activate it (recommended): python -m venv .venv

License
Choose a license and replace this line, e.g.: MIT License — see LICENSE file for details.

Credits
Created by @veeti-21
